<configuration>
  <appSettings>
    <!-- Defaults to $(MyDocuments)/dev if left empty -->
    <add key="dbPath" value="" />
    
    <!-- Defaults to Hestia.db if left empty -->
    <add key="dbName" value="hestia.db" />
  </appSettings>
</configuration>